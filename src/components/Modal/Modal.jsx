import { Overlay, ModalWindow } from './Modal.styled';

const Modal = () => {
  return (
    <Overlay>
      <ModalWindow class="modal">
        <img src="" alt="" />
      </ModalWindow>
    </Overlay>
  );
};

export default Modal;
